import { TitleFormPipe } from './title-form.pipe';

describe('TitleFormPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFormPipe();
    expect(pipe).toBeTruthy();
  });
});
